import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  dropdown: {
    marginTop: 5,
  },
  dropdownContainer: {
    marginTop: 20,
  },
  addBox: {
    display: 'flex',
    flexDirection: 'row',
  },
});
